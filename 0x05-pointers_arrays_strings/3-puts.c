#include "main.h"
/**
 * _puts - prints a string, followed by a new line, to stdout
 * @str: string to print
 *
 * Return: string and new line
 */

void _puts(char *str)
{
	int i = 0

	while (*str 1 = '\0')
	{
	_putchar(*str++);
	}
	_putchar('\n');
}
