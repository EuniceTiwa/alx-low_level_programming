#include "main.h"

/**
 * _islower - command to checks for lowercase character
 * @c: the character to check
 * Return: 1 if c is lowercase, if not then 0
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
